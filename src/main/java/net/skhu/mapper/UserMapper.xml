<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="net.skhu.mapper.UserMapper">
	<select id="findOne" resultType="User">
		SELECT * FROM user WHERE id = #{ id }
	</select>
	
	<select id="findOnewithNickName" resultType="User">
		SELECT * from user WHERE nickName = #{ nickName }
	</select>
	
	<select id="findAll" resultType="User">
		SELECT * FROM user
	</select>
	
	<select id="findAllwithState" resultType="User">
		SELECT * FROM user WHERE stateId = 1
	</select>
	
	<insert id="insert" useGeneratedKeys="true" keyProperty="id">
		INSERT INTO user(nickName, password) 
		VALUES (#{nickName}, #{password})
	</insert>
	
	<update id="update">
		UPDATE user 
		SET stateId = #{stateId}, win = #{win}, lose = #{lose}, rate = #{rate}
		WHERE id = #{id}
	</update>
	
	<delete id="delete">
		DELETE FROM user WHERE id = #{id}
	</delete>

</mapper>